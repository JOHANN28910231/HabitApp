/*
  Ejemplo de .env para tests. Copiar a `.env.test` y ajustar credenciales reales.
*/
NODE_ENV=test
PORT=3001

SESSION_SECRET=secret_for_tests_change_me

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=TuPasswordDePruebas
DB_NAME=habitapp_test

TRUST_PROXY=false

// Nota: importar `db/init.sql` al correr tests o usar `tests/helpers/reset_db.js` en hooks antes de tests.
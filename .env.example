# =========================================
# AppTiziHause - Configuraci�n de entorno
# Copia este archivo a ".env" y rellena valores locales.
# �NO subas tu .env al repositorio!
# =========================================

NODE_ENV=development
PORT=3000

# Sesiones (RF07) - usa algo largo y aleatorio en tu .env real
SESSION_SECRET=pon_aqui_un_secreto_muy_largo_y_aleatorio

# Base de Datos MySQL (esquema habitapp)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=dev_user
DB_PASS=dev_pass
DB_NAME=habitapp

# Proxy/HTTPS en producci�n (RF031)
TRUST_PROXY=false

# Correo / Notificaciones (RF028, RF029, RF020)
MAIL_PROVIDER=
MAIL_FROM="AppTiziHause <no-reply@apptizihause.local>"
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Simulaci�n de pagos (RF021�RF024)
PAYMENTS_MODE=mock
PAYMENTS_FORCE_STATUS=aprobado

# L�mites y est�ticos
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
JSON_LIMIT=1mb
PUBLIC_DIR=public
